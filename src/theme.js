import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#313866",
            contrastText: "#FE7BE5"
        },
        testColor: {
            main: "#F00",
            hover: "#32F"
        }
    }
})